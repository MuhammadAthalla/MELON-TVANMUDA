name: ujikomtvanmuda
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  auto_size_text: ^3.0.0
  auto_size_widget: ^0.0.4
  flutter:
    sdk: flutter
  flutter_icon_shadow: ^2.0.6
  flutter_svg: ^2.0.10+1
  google_fonts: ^5.1.0
  gradient_borders: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/svg/
